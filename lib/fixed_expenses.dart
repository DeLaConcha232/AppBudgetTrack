import 'package:flutter/material.dart';

// ignore: camel_case_types
class Fixed_expenses extends StatelessWidget {
  const Fixed_expenses({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Material App',
      home: Scaffold(
        appBar: AppBar(
          title: const Text('Material App Bar'),
        ),
        body: const Center(
          child: Text('Hello World'),
        ),
      ),
    );
  }
}